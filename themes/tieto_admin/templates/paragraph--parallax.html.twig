{%
set classes = [
	'paragraph',
	'paragraph--type--' ~ paragraph.bundle|clean_class,
	view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
]
%}

<div{{ attributes.addClass(classes) }} data-parallax="scroll" data-natural-width="100%" data-bleed="50" data-position="0px -50px" data-image-src="{{ file_url(content.field_image_demo['#items'].entity.uri.value) }}" style="min-height: {{ content.field_minimum_height[0]['#markup'] }}">
  <section class="paragraph-inner">
    {{ content.field_text_demo }}
  </section>
</div>