langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_menu
    - node.type.campaign
  module:
    - link
id: node.campaign.field_menu
field_name: field_menu
entity_type: node
bundle: campaign
label: Menu
description: 'You can set anchor links (e.g. <strong>#contact-us</strong>) or external links (e.g. <strong>https://tieto.com</strong>). Be sure that the anchor link matches with one of the section''s <em>Anchor</em> field. To delete a link, just empty the inputs and Save the page.'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  link_type: 17
  title: 2
field_type: link
